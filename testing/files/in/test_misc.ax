# compound comparison
x <= y <= z

# interpolation
x = f'Hello, {name}!'

# 1-36 based number
#x = 36::h87zw29::i32

# unpacking
() = Object.(a, b, c)

Seq.each (["one", "two", "three"], fn (s): IO.write(s))

for x in range(0, 10): Console.WriteLine(x)

"this is a long text" |> capitalize |> shortenText

person |> parseData |> getAge |> validateAge

# branches
if true { } else { 'Fail' |> Console.WriteLine }

unless 0 == 0 { 'OK' |> Console.WriteLine } else { 'Fail' |> Console.WriteLine }

if true { } else { 'Fail' |> Console.WriteLine }

if true { Console.WriteLine('true') } else { Console.WriteLine('false') }

x: int = 42 if Math.Max(0, 1) == 1

print(1 if 1 == (2 if 3 == 4 else 2) else 3)

x = (1, 2) if 1 == 4 else (3, 4)

x = a if a > b else b

# loops
for (x, y) in zip(range(1, 10), range(9, 0)): print(x, y)

for x in range(1, 10): print(x)

x = 0
until x >= 10 { Console.WriteLine(x) }

# 'with'
# with A() as a, B() as b: suite
# with f = file.open(r"C:\a.txt")
#     f.write(0b1)

# generators
g = (yield i for i in range(5))
g = ((yield i) for i in range(5))

# async/await
await (GetStringAsync(), GetGuidAsync())

# multiple assignment
x = y = z = 3

# match
x match
Email (email, title, _):
	'1'
SMS (number, message):
	'2'

# obj initializer
new Email[Address, Title]() { A = 2, B = 4 }

let l = new List[Str]()

# function as object
x = fn (arg1: int, arg2: bool) { print(arg1, arg2) }

x: Action = fn { Console.WriteLine('Hello, world!') }

cube: Func[int] = fn (x: int) { x * x * x }

cube: Func[BigInteger, BigInteger] = fn (x: BigInteger) { x * x * x }
Console.Write(cube(9999999999999999999))

# TODO: make test
x = true
y = false
z = true
a = x or y and z
b = x || y && z
Console.WriteLine(x)
Console.WriteLine(y)
Console.WriteLine(z)
Console.WriteLine(a)
Console.WriteLine(b)