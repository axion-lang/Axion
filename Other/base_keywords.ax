# access modifiers:

open	# accessible anywhere include other assemblies
inner	# accessible anywhere in current assembly
local	# accessible only in it's local space (e.g. class)

# other

init	# constructor method definition
empty	# null - equivalent (undefined value)
typeof