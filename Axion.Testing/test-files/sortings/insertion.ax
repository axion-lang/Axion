# import random
# from operators import lt, gt
#
# A = list(range(10))
# random.shuffle(A)
# print(A)
# reverse = False
# compare = gt if reverse else lt
#
# for j in range(1, len(A)):
#     j = i
#     while j > 0 and compare(A[j], A[j - 1]):
#         A[j], A[j - 1] = A[j - 1], A[j]
#         j -= 1
#
# print(A)


import
  random
  operators.(lt, gt)

A = [0..10]
random.shuffle(A)
print(A)
reverse = False
compare = gt if reverse else lt

for i in 1...len(A)
  for j = i;  j > 0 and A[j] compare A[j - 1];  j--
    swap A[j], A[j - 1]

print(A)
