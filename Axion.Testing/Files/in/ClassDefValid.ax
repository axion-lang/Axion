#class Point ([private] x: num, [private] y: num) < mod1.base1, m2.base2 { }

class Point < mod1.base1, m2.base2
    #[private] {
    #    [static, react] x: num
    #    y: num
    #}

    #[static]
    fn FromCoords (x: num, y: num) => Point
        return Point (x, y)